#include <stdio.h>

void main (){
    // int arr[2];
    // arr[0]=23;
    // arr[1]=45;
    // printf("%d",arr[0]);

    // Another way to initialise the array 

    int array[]={22,34,41,21,34};
    printf("%d",array[0]);
    for (int i = 0;i<5;i++){
            printf(" %d",array[i]);
    }
}